
Route::get('/{{routepath}}', {{class}}::class)
    ->middleware(['web', 'auth'])
    ->name('{{routename}}')
    ->crumbs(fn ($crumbs) => $crumbs->parent('{{homeroute}}')->push('{{routetitle}}', route('{{routename}}')));
