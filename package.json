{
    "private": true,
    "scripts": {
        "dev": "vite build --watch --mode rapyd",
        "build": "vite build --mode rapyd && vite build --mode bootstrap",
        "prod": "npm run build"
    },
    "devDependencies": {
        "bootstrap": "^5.3.3",
        "bootstrap-icons": "^1.11.3",
        "sass": "^1.90.0",
        "vite": "^7.1.0"
    },
    "dependencies": {
        "bootstrap-modbox": "^1.6.2",
        "livewire-sortable": "^1.0.0",
        "quill": "^2.0.1",
        "tom-select": "^2.1.0"
    }
}
